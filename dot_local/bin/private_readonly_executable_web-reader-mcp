#!/bin/bash

export Z_AI_API_KEY="$(pass-cli get z-ai/api-key --quiet -f password)"

exec uvx mcp-proxy --transport streamablehttp \
  --headers Authorization "Bearer $Z_AI_API_KEY" \
  https://api.z.ai/api/mcp/web_reader/mcp "$@"
